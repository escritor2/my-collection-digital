## Nuxt runtime env (client-exposed)
## Use prefix NUXT_PUBLIC_ to expose to browser

# development | staging | production
NUXT_PUBLIC_APP_ENV=development

# Backend base URL (Laravel)
# Leave empty in local dev to infer host automatically:
# e.g. frontend localhost:3002 -> backend localhost:8000
NUXT_PUBLIC_API_BASE_URL=

